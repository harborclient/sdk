name: CI

# This repository is archived. CI is disabled; development continues in
# https://github.com/harborclient/harborclient (packages/sdk).

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Repository archived
        run: |
          echo "SDK development moved to harborclient/harborclient packages/sdk."
          echo "See https://github.com/harborclient/harborclient"
