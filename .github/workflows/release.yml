name: Release

# This repository is archived. Releases are published from
# https://github.com/harborclient/harborclient via sdk-release.yml (sdk-v* tags).

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Repository archived
        run: |
          echo "SDK releases moved to harborclient/harborclient."
          echo "Run: pnpm release:sdk"
          exit 1
